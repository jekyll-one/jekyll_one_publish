NOTE:
--------------------------------------------------------------------------------

  Find server configuration settings (util_srv) with YAML data files
  from ~/_data/utilities.

  Find logger client settings (log4javascript) with YAML data files
  from ~/_data/modules.

  
